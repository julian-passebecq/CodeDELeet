# Evidence boundaries

These are actual CodeDELeet V2.2 test results, not copied V2.0 claims. Start with `test-results.json` and `gallery.html`. Current migration/audit/report documentation is in `docs/` of the source package. Inherited V2.0 records are explicitly under `docs/history/`.

A built-file Chromium harness was required because URL navigation is blocked by the execution environment. It runs actual compiled UI modules and real CodeMirror/teaching engines with a file/storage transport adapter. It does not simulate successful DuckDB/Pyodide/Mermaid loading. `network-runtime-report.json` is UNVERIFIED and its 0/0 counters must not be read as a passing suite. urllib-based static HTTP MIME/byte checks did run.

A registry DNS error blocked clean npm installation. The preinstalled TypeScript version is exactly 5.8.3, as locked. No current package audit is claimed. `npm-ci.log` retains the actual error.

Screenshots are automated test captures from the compiled app. Some contain distinctive test drafts or transient test notifications; these are not stock mockups or intended interview answers. The 390px phone workspace was visually checked after its overlay restoration fix.
