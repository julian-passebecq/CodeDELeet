# Historical non-runtime input omitted from the V2.3 release

The supplied V2.2 repository archive contained `leetcodedataeng-main.zip` here.
It is a historical, separate trainer input, not a dependency of CodeDELeet V2.3.
The V2.3 source/build release deliberately omits the nested archive. Existing
learning content, engines, fixtures, tests and runtime dependencies are retained.
No archive from the new private offline implementation library is redistributed.
Historical descriptions under docs/history describe their original inputs only.
